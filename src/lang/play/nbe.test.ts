// import {readBack} from "../nbe"
// import {Parser} from "../parser"
// import {evaluate} from "../evaluate"
//
// const parser = new Parser()
//
// const code = `
// ((lambda (x) (lambda (y) (x y))) (lambda (x) x))
// `
// const exp = parser.parseExp(code)
// const res = readBack(new Set(), evaluate(exp, new Map()))
// console.log(res)
